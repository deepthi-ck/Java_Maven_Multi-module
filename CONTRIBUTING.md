# Contributing

This branch targets **Java 21** only (release=21, bytecode major 65).

1. Use JDK 21 (see .sdkmanrc).
2. Run ./mvnw clean test before opening a PR.
3. Keep quality tool configs under config/ and scripts/ in sync with documentation/TOOLS.md.